#ifndef UNIFORM_LOADER_H
#define UNIFORM_LOADER_H

#include "AppContext.h"

// Loads and initializes all shader uniform locations into the application context
extern void loadUniforms(AppContext *ctx);

#endif
